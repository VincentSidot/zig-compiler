use64

; mov [1234h], eax
; mov [0xffff_ffff_ffff_ffff], eax

; mov [rax], rcx
; mov rax, [ebx + r12*2]
; mov rax, [r12]
; mov rax, [r11]
; mov rax, [rsp]
; mov rax, [al + bl]
; mov rax, [rax + r12*8]
; mov rax, [rax + rcx*8 - 4]
; mov rax, [rcx]

; mov ebx, [1234h]

mov [r8d], rax
